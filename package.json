{
  "name": "sina-street",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cross-env NEXT_DEV_CACHE=1 next dev --turbopack -p 3001",
    "dev:webpack": "cross-env NEXT_DEV_CACHE=1 next dev -p 3001",
    "dev:clean": "rm -rf .next-dev && cross-env NEXT_DEV_CACHE=1 next dev --turbopack -p 3001",
    "clean": "rm -rf .next .next-dev",
    "build": "cross-env NEXT_DEV_CACHE=0 next build",
    "start": "cross-env NEXT_DEV_CACHE=0 next start -p 3001",
    "demo": "cross-env NEXT_DEV_CACHE=0 next build && cross-env NEXT_DEV_CACHE=0 next start -p 3001",
    "lint": "cross-env NEXT_DEV_CACHE=0 next lint"
  },
  "dependencies": {
    "framer-motion": "^12.38.0",
    "lucide-react": "^1.14.0",
    "next": "15.5.18",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "cross-env": "^10.1.0",
    "eslint": "^9",
    "eslint-config-next": "15.5.18",
    "postcss": "^8",
    "tailwindcss": "^3.4.1",
    "typescript": "^5"
  }
}
